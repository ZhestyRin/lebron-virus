## features
- create restore script
- request keystroke simulations first and auto-accept other permissions
- use aforementioned simulation and listening to prevent itself to shut down comedically.
- or we could just make the mouse move around randomly(boring)
- insert itself into auto login items
- use `trap` to stop mild quit signals. figure out a way to bypass SIGTERM.
- insert itself into login policies
- maybe branch three versions: the standard version, the version which is harder to shut, and a version that requires `sudo`.
- make the opened pictures bounce around or shake, maybe even transparent background
- open spinning lebron james
- make the dock glitch between being at the left, right and bottom.
- shuffle desktop icons
- invert colors (maybe??? idk) or color tints
- kill switch.

## chores
- isolate the applescript to resources/ if it becomes long
- kill aerial companion
- show the dock & desktop via `defaults` to ensure a uniform experience
- don't `curl | bash` component scripts in the same repo, seems like bad practise
- maybe create a wrapper script that calls the main script `&>/dev/null`, disowns the process and exits the terminal
- reactivate the menu bar program by downloading a pre-compiled swift binary and letting you check the checksums first
- check files before overwriting even tho the risk is low
